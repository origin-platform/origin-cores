<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.jyusun.origin</groupId>
        <artifactId>origin-dependencies-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath/>
    </parent>

    <artifactId>origin-core-dependencies</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <name>${project.artifactId}</name>
    <packaging>pom</packaging>
    <description>公共基础依赖管理</description>

    <!-- 许可信息 -->
    <licenses>
        <license>
            <name>The Apache Software License, Version 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <comments>A business-friendly OSS license</comments>
        </license>
    </licenses>

    <!-- 开发者信息 -->
    <developers>
        <developer>
            <id>jyusun</id>
            <name>ayu</name>
            <email>jyusun@foxmail.com</email>
        </developer>
    </developers>

    <properties>

    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- 常用工具 -->
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>origin-core-common</artifactId>
                <version>${origin.development.version}</version>
            </dependency>
            <!-- 通用基础-基础模型 -->
            <dependency>
                <groupId>com.jyusun.origin</groupId>
                <artifactId>origin-core-model</artifactId>
                <version>${origin.development.version}</version>
            </dependency>
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>origin-core-tools</artifactId>
                <version>${origin.development.version}</version>
            </dependency>

            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>origin-core-db</artifactId>
                <version>${origin.development.version}</version>
            </dependency>
            <!-- 权限工具 -->
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>origin-base-secure</artifactId>
                <version>${origin.development.version}</version>
            </dependency>
            <!-- 对象存储工具 -->
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>origin-base-oss</artifactId>
                <version>${origin.development.version}</version>
            </dependency>
            <!-- Excel工具 -->
            <dependency>
                <groupId>${project.groupId}</groupId>
                <artifactId>origin-base-excel</artifactId>
                <version>${origin.development.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
